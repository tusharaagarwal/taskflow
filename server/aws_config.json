{
  "Version": "2012-10-17",
  "Id": "__default_policy_ID",
  "Statement": [{"Sid":"__owner_statement","Effect":"Allow","Principal":{"AWS":"arn:aws:iam::ACCOUNT_ID_PLACEHOLDER:root"},"Action":"SQS:*","Resource":"arn:aws:sqs:ap-south-1:ACCOUNT_ID_PLACEHOLDER:orchestrator-to-assembler"}],
  "region": "ap-south-1",
  "resources": {"sqs_queue": "orchestrator-to-assembler"},
  "_credentials_note": "Leave empty; use the AWS credentials refresh script with appropriate -Profile parameter to populate ~/.aws/credentials, then this consumer will use those credentials automatically. GitHub secret scanning flags these fields even when empty.",
  "_security_warning": "AWS credential fields are intentionally empty to avoid exposing sensitive data. These should be populated via environment variables or AWS credentials file, not committed to version control.",
  "credentials": {
    "access_key_id": "",
    "secret_access_key": "",
    "session_token": ""
  }
}